# Configuration for project
spring:
  profiles:
    include:
      - redis
      - kafka
      - github
  datasource:
    url: jdbc:mysql://gg-pigs.com:3306/gg_pigs_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: "비밀 값 입니다."
    password: "비밀 값 입니다."
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 2

  jpa:
    properties:
      format_sql: true
      default_batch_fetch_size: 100
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

  # Configuration for SMTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: pigs95team@gmail.com
    password: "비밀 값입니다."
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Configuration for application
application:
  admin:
    email: "pigs95team@gmail.com"
    emails:
      - "pigs95team@gmail.com"
      - "kihyun5741@gmail.com"
  mail:
    from: "pigs95team@gmail.com"
  cookie:
    login-cookie-name: "gg-login"
    login-cookie-domain: "gg-pigs.com"
  jwt:
    security-key: "비밀 값 입니다."
    expiration-age: 3600000